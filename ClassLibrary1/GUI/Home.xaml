<Page x:Class="GUI.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Home">

        <StackPanel Name="panel" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
        <Button Content="Nouvelle Partie" Click="newGame"/>
        <Button Content="Charger Partie" Width="{Binding
ElementName=panel, Path=Width, Mode=OneWay}" Click="loadGame"/>
        <Button Content="Quitter" Width="{Binding
ElementName=panel, Path=Width, Mode=OneWay}" Click="quit"/>
        </StackPanel>

</Page>
