<Page x:Class="GUI.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Home">
    <Page.Background>
        <ImageBrush ImageSource="images/riziere.jpg"/>
    </Page.Background>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="400"></RowDefinition>
            <RowDefinition Height="400"></RowDefinition>
        </Grid.RowDefinitions>

        <Image x:Name="Logo"  Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Source="images/logo5.png" Margin="0,0,1,300" MouseEnter="size_plus" MouseLeave="size_less"  ></Image>

        <StackPanel Grid.Row="1" Name="panel" VerticalAlignment="Center" HorizontalAlignment="Center" >
            <Button Content="Nouvelle Partie" Click="newGame"/>
            <Button Content="Charger Partie" Width="{Binding
ElementName=panel, Path=Width, Mode=OneWay}" Click="loadGame"/>
            <Button Content="Quitter" Width="{Binding
ElementName=panel, Path=Width, Mode=OneWay}" Click="quit"/>


        </StackPanel>
    </Grid>
    



</Page>
